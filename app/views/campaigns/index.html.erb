<h1>Fund.sy - Your Awesome Crowdfunding Solution</h1>
<h2>Recent Campaigns</h2>
<% @campaigns.each do |campaign| %>
 <%# using `cache` helper method. Rails will generate a fragement cache for this
     template. If you pass it an ActiveRecord object (such as `campaign`) it
     will use `cache_key` method to generate the cache key %>
  <% cache campaign, expires_in: 1.days do %>
    <div class="well">
      <h3>
        <%= link_to campaign.title.upcase, campaign %>
        (<%= campaign.rewards.size %> rewards)
      </h3>
    </div>
  <% end %>
<% end %>
